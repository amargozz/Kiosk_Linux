# return to CLI
Ctrl + Alt + F2
# return to GUI
Ctrl + Alt + F7

# hidden grub menu and no timeout
sudo nano /etc/default/grub
  GRUB_TIMEOUT=0
sudo grub-mkconfig -o /boot/grub/grub.cfg

# static IP
[Match]
Name=en01
[Network]
Address=192.168.1.100/24
Gateway=192.168.1.1
DNS=8.8.8.8

# disable USB ports
sudo nano /etc/udev/rules.d/99-usb-disable.rules
  SUBSYSTEM=="usb", ATTR{authorized}="0"
    # execute "sudo udevadm control --reload" then reboot
    # to revert just rename or delete the file



